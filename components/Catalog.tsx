import type { Media } from '@/lib/tmdb';
export function Catalog({title,items,id}:{title:string;items:Media[];id?:string}){return <section className="section" id={id}><div className="section-title"><h2>{title}</h2><a href="/search">View all →</a></div>{items.length?<div className="grid">{items.map(x=><a className="card" href={`/title/${x.id}`} key={x.id}><div className="poster"><img src={x.poster} alt="" loading="lazy"/><span className="play" aria-hidden>▶</span></div><div className="card-title">{x.title}</div><div className="meta">{x.year} · ★ {x.rating.toFixed(1)}</div></a>)}</div>:<div className="empty">No titles found.</div>}</section>}
